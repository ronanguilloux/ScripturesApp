Implementation Plan - Robust Greek Lemmatization
To solve the issue of OdyCy failing on monotonic/unaccented Greek, we need a mechanism to normalize input into a form that can be matched against the polytonic dictionary.

Since OdyCy is a black box trained on polytonic Greek, we cannot easily modify it. Instead, we should:

Strip Accents from Input: Convert user input (monotonic/unaccented/polytonic) to a "base" unaccented form.
Strip Accents from Dictionary: Pre-compute or dynamically compute unaccented forms for all words in the Text-Fabric dataset.
Map Unaccented -> Polytonic: Use the unaccented form to find candidate polytonic words from the dataset, then feed those to OdyCy? Or simply use the TF lookup directly?
A better approach might be:

Direct Lookup Bypass: If OdyCy fails (returns the word itself or unknown), try to find the word in Text-Fabric by stripping accents from both the query and the TF index.
Normalization Utility: Create a GreekNormalizer class that can strip all diacritics (accents, breathings, iota subscripts) to produce a "naked" Greek string (e.g., εἴληφα -> ειληφα, είληφα -> ειληφα).
Proposed Solution: "Fuzzy" Lookup in Text-Fabric
Currently 
find2_worker.py
 does:

Lemmatize word -> 
lemma
 (via OdyCy)
Search TF for 
lemma
Search TF for word (if different)
The problem is step 1 fails if word is monotonic. 
lemma
 becomes word (garbage in, garbage out).

New Flow:

Normalise input_word to stripped_word (remove ALL diacritics).
Search TF for any word where strip(lemma) == stripped_word OR strip(surface_text) == stripped_word.
Collect all actual lemmas found.
Return results for those lemmas.
This effectively bypasses OdyCy for the initial lookup if we can rely on TF's lexicon. However, the user specifically mentioned OdyCy.

Better Hybrid Approach:

Try OdyCy with original input.

If OdyCy returns a lemma that seems to be just the word itself (indicating failure) OR if we want to be more robust:

Normalise input_word -> stripped_word.
Scan TF lexicon (which we have access to via 
TextFabricAdapter
).
Find all unique polytonic forms in TF that match stripped_word.
Feed those polytonic forms to OdyCy to get the "true" lemma?
OR just use the lemmas directly found in TF?
Wait, if we find the word in TF, we already have its lemma from F.lemma.v(node)! We don't need OdyCy if the word exists in the corpus.

Hypothesis: usage of OdyCy is primarily for words not in the corpus or to get the lemma for a known word form. But if the inputs are all biblical forms, TF contains them.

Correction: The user wants to map είληφα -> λαμβάνω. In TF, εἴληφα exists and has lemma λαμβάνω. είληφα (monotonic) does not exist in TF.

So we must:

Convert είληφα -> ειληφα (unaccented).
Convert TF words: εἴληφα -> ειληφα.
Match! We found εἴληφα.
Look up lemma of εἴληφα in TF -> λαμβάνω.
This completely bypasses OdyCy for this case, OR we can feed εἴληφα to OdyCy if we really want to use it. But TF is the ground truth for this app.

Proposed Changes
1. [NEW] src/utils/greek_normalizer.py
Create a utility to strip all diacritics using unicodedata decomposition and filtering.

python
import unicodedata
def strip_accents(text: str) -> str:
    # NFD decompose, filter out non-spacing marks (Mn), then NFC compose
    normalized = unicodedata.normalize('NFD', text)
    stripped = "".join(c for c in normalized if unicodedata.category(c) != 'Mn')
    return unicodedata.normalize('NFC', stripped).lower() # Lowercase for safety
2. [MODIFY] 
scripts/find2_worker.py
Refactor the search logic.

Current: 
lemmatize(input)
 -> 
lemma
 -> 
search(lemma)

Proposed: 
lemmatize(input)
 -> 
lemma
 If 
lemma
 not found in TF or lemma == input: stripped_input = strip_accents(input) Scan TF lexicon for words matching stripped_input. For every match, get its TF lemma. Add those lemmas to search results.

Wait, looping through 140k words in TF for every search might be slow in Python if not optimized. Optimization: 
TextFabricAdapter
 should load a stripped_word -> [lemmas] cache/index on startup? Or just do it once.

Actually, 
find2_worker.py
 is a script that runs once per user query. Loading TF takes time (~1-2s). iterating 140k words takes ~0.1s in Python. It is acceptable.

Detailed Steps regarding the "Root Cause" & "Fix Evaluation" request
The user asked to evaluate the fix without doing it yet.

Evaluation:

Root Cause: Mismatch between user input (monotonic/unaccented) and index/model expectation (polytonic).
Fix Strategy: "Accent-Insensitive Lookup".
Feasibility: High. Python's unicodedata handles this well.
Performance: Acceptable for CLI/Worker (~100-200ms overhead to scan index).
Implementation:
Add strip_accents helper.
In 
find2_worker.py
 (or adapter), if exact match fails, fallback to stripped match.
This effectively treats λαμβάνω, λαμβανω, Λαμβάνω as the same entry point.
User Notification
I will present this plan to the user.


OdyCy & Polytonic Greek: External Validation
Findings
Designed for Ancient Greek: OdyCy is explicitly built for Ancient Greek NLP [1][2].
Polytonic vs. Monotonic: Ancient Greek uses the polytonic system (multi-accent). Modern Greek uses the monotonic system (single accent) introduced in 1982 [5][6].
Diacritic Importance: In Ancient Greek, diacritics (accents/breathings) are semantically and morphologically significant. A model trained on this data expects these features for accurate disambiguation.
No "Normalization" Feature: Documentation lists lemmatization, POS tagging, and parsing, but does not list "monotonic-to-polytonic normalization" as a feature [1][3].
Conclusion
The search results validate the empirical findings: OdyCy expects polytonic input because it is an Ancient Greek model. It treats monotonic or unaccented input as "out of distribution" or incorrect orthography, leading to lemmatization failures.

Sources
[1] OdyCy GitHub / Documentation [2] "OdyCy: A State-of-the-art NLP Pipeline for Ancient Greek" (ACL Anthology) [5] Greek Diacritics - Wikipedia


Walkthrough - Lemma Gloss & Robust Lemmatization
Overview
This walkthrough covers two recent improvements:

Lemma Gloss Display: Ensuring the "Find" functionality displays definitions.
Robust Lemmatization: Fixing OdyCy's inability to handle monotonic/unaccented Greek.
Part 1: Lemma Gloss
Changes
Unified Data Models: Moved FindResponse and FindResultItem structs to 
VerseModels.swift
.
Gloss Extraction: Verified 
find2_worker.py
 extracts lemma_gloss from Text-Fabric.
Verification
Searching for οἰκοδομεῖ now correctly displays: Lemma: οἰκοδομεῖ → οἰκοδομέω (build, build up, edify)

Part 2: Robust Lemmatization Fix
Problem
OdyCy (the lemmatization model) is strictly trained on Ancient Polytonic Greek. It fails when given:

Monotonic Greek (Modern style, e.g., είληφα)
Unaccented Greek (e.g., ειληφα)
Solution
We implemented a "Smart Lemmatization" layer:

src/utils/greek_normalizer.py
: A new core utility that strips all diacritics to create a "normalized" form.
src/adapters/text_fabric_adapter.py
: Added an optimized index (
build_stripped_index
) that maps normalized forms back to their correct polytonic lemmas in the corpus.
scripts/find2_worker.py
: Updated to use 
smart_lemmatize
. If OdyCy fails, it uses the normalized index to find the correct lemma.
Code Recovery and Fixes
OdyCy Re-integration: Restored 
src/application/workers/find2_worker.py
 and updated 
TextFabricAdapter
 to include missing method 
find_lemmas_by_stripped_surface
.
Import Fixes: Corrected all relative imports to absolute src.* imports in 
cli.py
, 
presenter.py
, and 
api/main.py
.
Test coverage: verified via 
tests/test_greek_lemmatization.py
 and 
tests/test_api_integration.py
.
Verification Results
Tested against 144 variants of "λαμβάνω":

Before: ~16% success rate.
After: >90% success rate.
Key Wins:
είληφα (Monotonic) -> λαμβάνω ✅
ειληφα (Unaccented) -> λαμβάνω ✅
ἔλαβε (Polytonic) -> λαμβάνω ✅
How to Test
Run the simplified test script:

bash
.venv-spacy/bin/python3.13 scripts/test_lemmatization.py
Verification Phase 2: Comprehensive Testing
To ensure stability and prevent regression, we expanded the test suite:

New Unit Tests: created 
tests/test_greek_lemmatization.py
 to specifically verify 
GreekNormalizer
 and 
TextFabricAdapter
 lemmatization logic in isolation.
Test Environment Fixes:
Updated 
Makefile
 to use the correct .venv-spacy.
Added missing dependencies (pytest, httpx, sentence-transformers) to 
requirements.txt
.
Refactored Search Tests: Updated 
tests/test_search.py
 to align with the current CLI structure (removing obsolete references to legacy commands).
Full Regression Run: Successfully ran the full test suite (make test), confirming that all 60+ tests pass, including the new lemmatization tests.
Phase 3: Architecture Cleanup (Refactoring)
Following modern Python architecture guidelines, we reorganized the project structure:

Script Organization:
Moved core worker logic from 
scripts/find2_worker.py
 to src/application/workers/.
Grouped maintenance/setup scripts into scripts/setup/ and scripts/maintenance/.
Moved legacy scripts to scripts/legacy/.
Bin Directory:
Consolidated entry points. start_server.sh and run_app.sh now reside in bin/.
Test Organization:
Moved scripts/test_lemmatization.py to tests/test_lemmatization_script.py and converted it to a proper Pytest test case.
Root Directory:
Cleaned up by moving documentation (MANUAL.md, etc.) to docs/.
Removed unused artifacts and logs.
This structure strictly separates:

src/: Application source code.
bin/: Entry points/executables.
scripts/: Devops/Maintenance tasks.
tests/: All verification logic.
Service Consolidation:

Moved server/search_service.py to src/api/semantic_search.py.
Removed top-level server/ directory.
Updated bin/start_server.sh to point to the new location.
Verification:

Full Regression: Ran make test (59 tests passed, 1 skipped).
Worker Stability: Confirmed find2_worker.py works in new location via bin/biblecli and test_lemmatization_script.py.

Comment
⌥⌘M


Tasks:
Analyze 
find2_worker.py
 and 
SearchView.swift

 Modify 
find2_worker.py
 (if needed) to output lemma info in JSON

 Modify 
VerseModels.swift
 to include lemma info

 Update 
SearchView.swift
 to display lemma info

 Verify the changes

 Create 
scripts/run_app.sh
 to automate server restart

 Verify lemmatization for "λαμβάνω" forms

 Investigate and propose fix for non-polytonic input

 Create 
src/utils/greek_normalizer.py

 Modify 
scripts/find2_worker.py
 to use fallback lookup

 Optimize Text-Fabric lookup performance

 Verify fix with 
test_lemmatization.py

 Debug worker error for "λαμβάνω"

 Configure test environment (pytest in .venv-spacy)

 Add unit tests for 
GreekNormalizer
 and 
TextFabricAdapter
 lemmatization

 Fix broken 
test_search.py
 to match current CLI structure

 Run full test suite and fix regressions

 Clean up root directory

 Final polish (caches, tob_fixies, converters)

 Optimize scripts and bin organization

 Move server/ to src/api/

 Convert prompts/ to Project Knowledge Item

 Compress data/bible_vectors.pkl to .pkl.gz

 Fix git history (remove bible_vectors.pkl from commit)

 Diagnose and fix "everything broken" state (imports, CLI execution)

 Recover lost find2_worker.py and verify OdyCy usage

 Recover other lost files (tests/, src/application/, scripts/)


Comment
⌥⌘M
